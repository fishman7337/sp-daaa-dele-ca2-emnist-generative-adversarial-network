# Split Notebook Parts

This folder contains source-only notebook sections generated from `notebooks/DELE_CA2_A (8).ipynb`.

Regenerate the files with:

```bash
python scripts/split_notebook.py --source "notebooks/DELE_CA2_A (8).ipynb" --output notebooks/parts
```

The split notebooks are for easier review and navigation. The original notebook remains the preserved source.
