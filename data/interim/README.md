# Interim Data

Use this folder for temporary transformed data, cached arrays, and intermediate preprocessing outputs.

Files here should be reproducible from raw data and code, so they are ignored by git.
