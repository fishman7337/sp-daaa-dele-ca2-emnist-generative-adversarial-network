# GitHub Configuration

This folder contains repository automation assets for issues, pull requests, CI, security scanning, and dependency maintenance.

Keep workflow changes small and verify them on a branch before relying on them on `main`.
