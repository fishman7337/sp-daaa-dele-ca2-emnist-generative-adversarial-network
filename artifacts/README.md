# Artifacts

This folder is reserved for local training outputs such as model checkpoints, experiment logs, and exported run artifacts.

Generated artifacts are intentionally ignored by git. Track only lightweight placeholders and documentation unless a release process explicitly requires otherwise.
