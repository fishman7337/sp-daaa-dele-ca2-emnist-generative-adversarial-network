# Models

Store local model checkpoints and exported model files here.

Model binaries are ignored by git. Use a dedicated model registry, release asset, Git LFS, or DVC workflow before publishing large trained artifacts.
