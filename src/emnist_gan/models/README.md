# Model Builders

This folder contains optional TensorFlow/Keras model-building utilities.

TensorFlow is not required for the default test suite. Install the ML extras before using these builders:

```bash
python -m pip install -e ".[ml]"
```
